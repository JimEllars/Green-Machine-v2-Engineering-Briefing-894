I have completed the user's tasks:
1. Standardize Worker Resolution & Remove Orphaned Diff: Update src/hooks/useSystemDiagnostics.js to use getWorkerUrl() and remove patch.diff.
2. Clean Catch-All Router & Surface KV Cache Ratio: Remove duplicate url.pathname !== "/api/telemetry" in thirdweb_bridge.ts, and add kv_cache_ratio (percentage) to /api/telemetry and /api/health.
3. Sprout KV Cache Efficiency Gauge in Telemetry Drawer: Update src/components/planner/SystemDiagnosticsPanel.jsx to show the KV Cache Hit-Rate gauge in the Deep Telemetry Inspector drawer based on edge_telemetry.kv_cache_ratio.
4. Add Real-Time Auth Pulse to System Health Snapshot Card: Update src/App.jsx to import subscribeToAuth and getSessionState, add an "Auth Session" badge to the System Health Snapshot Card next to the Oracle Link, and use useEffect and subscribeToAuth to update dynamically.

I ran tests (`tsc` on `edge-ledger-worker` and `lint/build`), fixed TypeScript type issues (duplicate `uptimeSeconds`), and visually verified the UI with Playwright.
Please provide feedback on my changes.
